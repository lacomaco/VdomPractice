1. MVC 패턴을 적용해서 현재 Laco에 묶여있는 Controller,View,Model을 모두 분리시키자.
그렇게 되면 구조가 더 깔끔해져서 메모리누수가 발생하는 부분을 확인할 수 있을것같다.

LacoModel -> props,state,Hooks, 라이프사이클에 들어가는 함수 데이터들을 관리
LacoController -> use* 함수들 제공, LacoView-LacoModel을 바인딩하여 관리
LacoView -> 실행할 JSX 함수와, 가상돔을 저장하고 관리,업데이트의 역할도 담당

2. 라이프사이클 가로채기

Babel로 바로 헬퍼 함수를 부르는게 아닌

JSX를 JSON으로 변경한후 라이플 사이클 훅을 주입시켜 헬퍼 함수를 호출하는 방식으로 수정


현재 문제점 DOM 삭제시 유지가 안됨

생성한 V-DOM에 State,Hooks 데이터를 내장하는 방법으로 해결해야할것 같다.
V-DOM에는 data property가 있음으로 그곳에 

data.data.hooks (state,useEffect 함수 저장)
data.data.afterHooks (cleanUp 함수 저장)
data.data.cursor (hooks index를 가리키는 함수)

3개의 프로퍼티를 심어놓고 관리한다.
-- 로직

domEntry : 함수형 컴포넌트를 렌더링 하는 최 상단 함수를 가리킨다.
rootDom : 생성된 가상 컴포넌트의 root를 의미한다.
domPointer : rootDom에서 현재 어느 DOM에 매칭되는지를 가리키는 변수를 의미한다.

bufferHooks : 임시 State들이 저장되는 위치
bufferAfterHooks : 임시 afterHooks들이 저장되는 위치


1. domPointer가 undefined 이라면 없는 V-Dom을 생성해야 한다는 의미가 된다.
1.1 V-DOM을 생성하기 전에 state,hooks들을 bufferHooks에 저장한다.
1.2 자신을 생성할때 buffer 데이터들을 V-Dom에 옮긴후 생성한다.

2. domPointer가 undefined가 아니라면 이미 있는 V-Dom을 생성한다는 의미가 된다.
2.1 useState,useEffect 들은 저장되어있는 data.hooks에서 데이터를 리턴하거나 실행한다.

